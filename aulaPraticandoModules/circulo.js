function area (raio) {
    return Math.PI * raio ** 2; // PI letra maiuscula 
}

module.exports = {
    area
}

function circunferencia (raio) {
    return 2 * Math * raio;
}

module.exports = {
    area,
    circunferencia
}    